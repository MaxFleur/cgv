#version 450

layout(local_size_x = 128) in;

layout(std430, binding = 0) buffer plot_buffer {
	ivec4 rgba[];
} plot;

uniform ivec2 resolution;

void main() {
	int idx = int(gl_GlobalInvocationID.x);

	if(idx < resolution.x * resolution.y)
		plot.rgba[idx] = ivec4(0);
}
